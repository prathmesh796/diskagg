{
  "name": "extension",
  "version": "1.0.0",
  "description": "Detect Kaggle activity and send to local RPC bridge",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w",
    "build": "del-cli dist && tsc && copyfiles \"manifest.json\" \"src/popup.html\" \"icons/**/*\" dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.210",
    "copyfiles": "^2.4.1",
    "del-cli": "^7.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
